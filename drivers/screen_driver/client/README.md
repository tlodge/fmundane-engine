# Client code for the screen driver

To get going run:

```
npm install
npm run build
```

And if you want the face recognition stuff to use locally served models then do a:

```
sh ./install-local.sh
```
