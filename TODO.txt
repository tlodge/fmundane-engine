Reference layers from other layers and navigation meta language (layer1:-1) (layer1:pause)  (layer1:id]) eg. to have control over back / forth
Timeouts (i.e. nodes that automatically progress after a timeout period) - need to be rendered in the tree.

show placeholders / edit placeholders in WoZ interface
show error reporting in WoZ interface
write driver for mini screens
upload support for media
flickering lights suppprt (or timings support!)
check if superflous code in screen driver
speech recognition in lenovo? -- could pass on to miniscreens?
speech synthesis lenovo?
driver for databox
